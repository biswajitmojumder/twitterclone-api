spring:
  datasource:
    url: jdbc:mysql://localhost:3306/twitterclonedb
    username: root
    password: fatih0001

twitterclone:
  cloud:
    redis:
      host: localhost
      port: 7001

logging:
  level:
    org:
      springframework:
        boot:
          autoconfigure:
            security: WARN